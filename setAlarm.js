function setAlarm(employed, vacation)
{
  //Recibo dos parametros
  //Employed es true cada que estoy employed? xd wtf
  //Vacation es true cuando estoy de vacaciones xd? 
  
  //La funcion tiene que ser TRUE si estas employed y no de vacaciones 
  //Else return false

  
  if(employed == true && vacation == false)
    {
      return true;
    }
  else
    {
      return false;
    }

 }
